{# Component Variables #}
{% set jumbotron_name = jumbotron_name|default('') %}
{% set jumbotron_color = jumbotron_color|default('#E5E5E5') %}

<div class="jumbotron" style="background: {{jumbotron_color}}">
    <div class="container">
        <h1>{{jumbotron_name}}</h1>
    </div>
</div>
